# @ihxy/orcha-core

Orcha — workflow **JSON** and **node IO catalog** parse, validate, and Document. No Vue / Vue Flow.

```bash
pnpm add @ihxy/orcha-core
```

Schema and fixtures: [`contracts/`](./contracts/).

```ts
import {
  parseNodeCatalog,
  parseWorkflow,
  toRuntimePayload,
  validateWorkflowAgainstCatalog,
} from '@ihxy/orcha-core'
```

## License

Apache-2.0.
