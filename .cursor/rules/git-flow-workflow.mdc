---
description: Git Flow 工作流确认、分支规范与 commit 消息格式（改代码/操作 git 时必须遵守）
alwaysApply: true
---

# Git Flow 与 Commit 规范

改代码或操作 git 前必须遵守本规则。本规则按仓库**实际存在的分支**执行，不要求每个仓库都具备完整链路。

## 开工前确认（不可跳过）

用户要求改代码时，即使已能判断意图，也必须先确认：

1. 本次是 **feat** 还是 **fix** 工作流
2. 新分支命名（须给出示例供用户确认或修改）

确认前先查看本仓库远程/本地分支，再按下方「环境分支」解析出实际签出源与合并链，写进确认话术。

确认话术示例：

> 本次按 **feat** 流程：从 `dev` 签出 `feat/user-email-login`，合并回 `dev`。分支名是否 OK？
>
> 或按 **fix** 流程：从 `main` 签出 `fix/order-payment-timeout`，再依次合并到 `main` → `pre` → `test` → `dev`（跳过本仓库不存在的分支）。分支名是否 OK？

未确认前不要建分支、改代码或提交。

## 环境分支

标准环境链（从开发到生产）：

```
dev → test → pre → main
```

| 环节 | 常见分支名 | 说明 |
|------|------------|------|
| 开发 | `dev` / `develop` | 日常开发与 feat 合并目标 |
| 测试 | `test` | 测试环境；没有则跳过 |
| 预发 | `pre` / `staging` | 预发布；没有则跳过 |
| 生产 | `main` / `master` / `prod` | 以仓库默认生产分支为准 |

**解析规则（每次操作前执行）：**

1. 列出远程分支，按上表识别本仓库实际有哪些环境分支
2. 生产分支优先取仓库默认分支；若默认不是生产名，再在 `main` / `master` / `prod` 中取实际存在的那个
3. **不存在的环节直接跳过**，不要新建环境分支（除非用户明确要求）
4. 若只有生产分支、没有 `dev`：feat 与 fix 均从生产分支签出，合并回生产分支

## Git Flow

做任何分支操作前，必须先 `pull` 本次会用到的远程分支。若操作会触及多条环境分支，凡本仓库存在的环境分支都 pull 到最新。

| 场景 | 签出源 | 分支名 | 合并目标 |
|------|--------|--------|----------|
| 新需求 | 开发分支（无则用生产） | `feat/xxx` | → 开发分支（无则 → 生产） |
| 正式环境 Bug | 生产分支 | `fix/xxx` | → 生产，再**逆序**回灌：`main → pre → test → dev`（跳过不存在的） |
| 需求上线 | — | — | 沿环境链正向：`dev → test → pre → main`（跳过不存在的） |

合并时只对**实际存在**的相邻（或次相邻）分支操作；话术与操作记录里写清跳过了哪些分支。

## Commit 规范

格式：`<type>(<scope>): <description>`

- **type**：`feat` | `fix` | `docs` | `style` | `refactor` | `perf` | `test` | `build` | `ci` | `chore` | `revert`
- **scope**：本次影响的模块（如 `user`、`order`）
- **description**：中文，动词开头
- 非极简单变更必须有 Body：标题后空一行，用 markdown 无序列表分点；可用 `**加粗**`

```
feat(order): 支持批量订单导出

- 新增 Excel 导出接口
- 新增异步导出任务
- 新增导出记录管理
```

```
fix(order): 解决支付超时的问题
```
