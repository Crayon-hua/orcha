# 物料

`@ihxy/orcha-materials` 提供内置节点：**开始**、**结束**、**任务**、**条件**。

本页为占位，内容稍后补充。

| type | 作用 |
| --- | --- |
| `start` | 流程入口，仅出口 |
| `end` | 流程出口，仅入口 |
| `task` | 一进一出 |
| `condition` | 一进，`true` / `false` 两出 |
